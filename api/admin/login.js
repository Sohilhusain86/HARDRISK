const {bcrypt,sign}=require('../lib/auth'); const {json,method,body,error}=require('../lib/http');
module.exports=async(req,res)=>{try{method(req,'POST');const b=body(req);if(!process.env.ADMIN_EMAIL||!process.env.ADMIN_PASSWORD_HASH)throw new Error('ADMIN_NOT_CONFIGURED');if(String(b.email).toLowerCase()!==process.env.ADMIN_EMAIL.toLowerCase()||!(await bcrypt.compare(String(b.password||''),process.env.ADMIN_PASSWORD_HASH)))throw new Error('INVALID_LOGIN');const token=sign({id:'admin',email:process.env.ADMIN_EMAIL,role:'admin'});return json(res,200,{ok:true,token,admin:{email:process.env.ADMIN_EMAIL}})}catch(e){return error(res,e)}};
