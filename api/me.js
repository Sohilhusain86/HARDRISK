const {verify,getUser,safeUser}=require('./lib/auth'); const {currentPlan,getPlan}=require('./lib/plans'); const {firebase}=require('./lib/firebase'); const {json,method,error}=require('./lib/http');
module.exports=async(req,res)=>{try{method(req,'GET');const c=verify(req),u=await getUser(c.sub),plan=currentPlan(u),p=getPlan(u),day=new Date().toISOString().slice(0,10),s=await firebase().ref(`usage/${u.id}/${day}/messages`).get();return json(res,200,{ok:true,user:safeUser({...u,plan}),plan:p,usage:{count:Number(s.val()||0),limit:p.dailyMessages}})}catch(e){return error(res,e)}};
